pages:
  image: node:latest
  stage: deploy
  environment: production
  cache:
    key: build-cache
    paths:
      - node_modules/
  script:
    - npm i
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
